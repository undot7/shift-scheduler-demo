# CHANGELOG

## [1.6.2] - 2026-08-14

v1.6.1 に対する reviewer の再レビューで出た**指摘4件**＋**オーナー確定の追加修正3件**。変更は `index.html` と `README.md` のみ（`demo-data.js`・`STATE_KEY`（`ssd:state:v6`）・`VIEW_KEY`（`ssd:view:v2`）は変更なし＝**再シードは起きない**）。自動生成ロジックには一切触れていないため、アプリ内のバージョン表記は **v1.6 のまま据え置き**。

### 修正（reviewer 指摘）
- **① 入社日（`joinDate`）側にも保存時の後処理を入れた**（`saveStaff`）。v1.6.1 で退職日側にだけ入れた「在籍期間外の希望を削除する」後処理が入社日側に無く、**既存スタッフに未来の入社日を設定すると退職日側とまったく同じ症状**（その月のグリッド・印刷に全セル「—」の幽霊行が残る／その月を公開すると本人に「シフト公開のお知らせ」が届く／在籍期間外の日は希望モーダルが開かないので本人も店長もその希望を消せない）が残っていた
  - 削除するのは `userId` 一致かつ **`入社日より前` かつ `今日以降`** の希望だけ。**過去の日の希望は消さない**（無制限に消すと公開済み月の有給が黙って消え、**扶養の累計見込み（`monthEarnOf` の有給分）と本人の公開済みカレンダーが無通知で変わる**ため）
  - シフト（実績）は退職日側と同じく**一切消さない**。入社日を空欄に戻す保存では何も削除しない
- **② 本人カレンダーの分岐順を店長グリッドに合わせた**（`staffShiftTab`）。有給チップを在籍判定より先に評価していたため、**同じ日が店長グリッドでは「—」・本人カレンダーでは「有給」**と食い違っていた（v1.6.1 の推奨⑤で「先頭で在籍外なら即『—』」はやめたが、順序が有給の後ろに入っていた）。店長グリッドと同じ「**シフト → 在籍外なら「—」 → 有給 → 公休**」の順に揃えた
- **③ 無効化店舗に所属したまま在籍者に戻す保存を止めるガードを追加**（`saveStaff` ガード6）。「店舗の全員に退職日 → その店舗を無効化 → 1名の退職日を消して復職」の手順（指示書 §8.1 12-E）を踏むと、その人は在籍者なのに**視点選択にもオーナーの個人設定一覧にも出てこない**（どちらも `activeStores()` を走査するため）状態になっていた。toast「**◯◯は無効化されています。先に所属店舗を変更してください**」（◯◯＝店舗名。スタッフ一覧は複数店舗を同時に表示するため名指しする）で中止する。**先に有効店舗へ異動させてから復職する**正規の手順は従来どおり通る（退職者のままの台帳編集も止めない）
- **④ 退職日側の警告 toast を指示書 §4.4.2 の表記に合わせた**。`退職日より後にシフト10件が残っています。…（退職日より後の希望3件は削除しました）` → **`退職日より後の希望3件を削除しました。シフト10件が残っています。シフト表から削除・整理してください`**（希望のみ・シフトのみ・どちらも0のときの文言は指示書の表どおり）

### 修正（オーナー・司令塔確定の追加分）
- **⑤ 入社日より前に残っているシフトも警告するようにした**（オーナー確定・指示書 D14）。退職日側にだけ「シフト◯件が残っています」の警告があり、**入社日側は無言だった**ため、在籍期間外のシフト（赤枠セル）が残っていることに店長が気づけなかった → **`入社日より前にシフト◯件が残っています。シフト表から削除・整理してください`**。**シフトは削除しない**（警告のみ）のは退職日側と同じで、**件数は日付の下限を置かず数える**（＝グリッドで赤枠になっているセルの件数そのもの）
  - あわせて **toast の組み立てを指示書 §4.4.2 の連結ルールに統一**した。並び順は**入社日側の希望削除 → 退職日側の希望削除 → 残シフト**、文どうしは **`。` で連結**（末尾に `。` は付けない）、**残シフトは必ず末尾の1文にまとめる**（両側に残っている場合は `入社日より前にシフト◯件・退職日より後にシフト◯件が残っています。…` と中黒で並べ、`シフト表から削除・整理してください` が2回出ないようにする）。退職日側の短縮形（`シフト◯件が残っています`）を使うのは **`jshN === 0` かつ `lrN > 0` のときだけ**（第1次改訂の確定文言を保つための例外）
- **⑥ 退職日側の希望削除にも「今日以降」の限定を入れて入社日側と対称にした**（オーナー確定）。v1.6.1 の退職日側は日付の下限が無く、**過去日の退職日を入れると過去月の有給まで消え、扶養の累計見込み（`monthEarnOf` の有給分）と本人の公開済みカレンダーが無通知で変わっていた** → `date > leaveDate` **かつ `date >= 今日`** に統一（①と同じ理由・同じ書き方）
- **⑦ ロールの段変更ガードの toast を指示書 §4.4.3 の文言に合わせた**（司令塔確定）。`◯◯さんが店舗に所属していないため、このロールの段は下げられません…` → **`このロールには店舗に所属していないユーザー（◯◯さん）がいるため、この段には変更できません（先に所属店舗を設定してください）`**。**ガードのロジックは変更していない**（文字列のみ）

### 検証
- **自動生成の決定性・退行なし**: 修正後に再実測し、seed=**`4741ef6af74541b8`**／生成後（3店舗×来月・全465件）=**`e5e2f4b1c57abb5d`** が v1.5.2・v1.6.0・v1.6.1 と**完全一致**（3回連続実行も一致）。①〜⑦はいずれも保存・表示の分岐と文字列のみで `generateForMonth` / `canAssign` には触れていない
- **自動テスト**: jsdom **292項目**（既存291項目のうち**旧契約を検査していた4項目を新しい契約に書き替え**＝③で止まるようになった経路1項目・④の旧文言3項目）＋ **今回の7件専用に新規作成した85項目**（うち**指示書 §4.4.2 の展開表11行をそのまま照合するケース**を含む）＝**377項目すべて成功**／実ブラウザ（Chrome・390px・file:// 直開き）**82項目**（既存29＋28に加え、今回ぶんの新規25項目）**すべて成功・console エラー0・横あふれ0**
- 主な実測: ①未来の入社日を設定 → 今日以降の希望だけ消え**過去の有給は残り、その月の見込み額が1円も変わらない**・来月グリッドに幽霊行なし・公開通知も飛ばない／②在籍外の日に有給が残っていても本人カレンダー・店長グリッド・店舗全体ビューの3画面すべてが「—」で一致（在籍中の日は従来どおり有給チップ）／③無効化店舗のまま復職しようとすると店舗名入りの toast で中止、有効店舗へ移せば復職できる／④⑤⑦文言が指示書と一致（⑤はシフトが0件なら警告を出さない・シフトは1件も消えない・該当セルは赤枠）／⑥過去日の退職日を入れても過去月の有給と見込み額が変わらない／⑧最長ケース（4カウンタすべて0でない）の toast も390px幅の画面内に収まる

### 既知の事項
- 希望の削除は入社日側・退職日側とも「今日以降」に限るため、**在籍期間外でも過去の日の希望はデータとして残る**（グリッド・カレンダーでは在籍外表記「—」で覆われるため画面上は見えない）。過去の記録・扶養累計を勝手に書き換えないことを優先した仕様
- 入社日より前のシフトの件数は**過去月の実績もすべて数える**（退職日側と同じ数え方）。既存スタッフに後から入社日を入れると件数が大きくなることがあるが、いずれも在籍期間外として赤枠で表示されるセルであり、掃除の対象として正しい件数
- v1.6.1 までの既知事項（退職者に残るシフトは実績として残す・転属者の所属履歴なし・月の公休日数を按分しない・既定では店長が時給を見られる）は据え置き

## [1.6.1] - 2026-08-14

v1.6.0（マスタ管理・在籍期間・権限）に対する reviewer 検証で見つかった**要修正4件・推奨4件**と、**オーナー確定の追加修正1件**への対応。変更は `index.html` のみ（`demo-data.js`・`STATE_KEY`・`VIEW_KEY` は変更なし＝**再シードは起きない**）。自動生成ロジックには一切触れていないため、アプリ内のバージョン表記は **v1.6 のまま据え置き**。

### 修正（要修正）
- **① ダッシュボードが退職した元店長を店長として表示していた**（`ownerHtml`）。`membersFor(...).find(u => u.role === "manager")` に `!retired` が無く、退職した元店長に来月の実績（自動生成した下書き）が残っていると、**ダッシュボードは「店長: 佐藤美咲（退職者）」・視点選択は「店長: 田中優奈」と同じ内容が2画面で食い違っていた**。`&& !retired(u)` を追加して両画面の判定を揃えた
- **② 退職者に希望だけが残ると幽霊行・公開通知が消せなかった**（`saveStaff`）。**退職日より後の希望（`requests`）は保存時に削除する**ようにした。シフトは実績なので**消さない**（従来どおり警告＋赤枠＋削除専用モーダルで店長が判断して掃除する）
  - 経緯: 在籍期間外の日は希望モーダルが開かないため、残った希望を**画面から消す手段が存在しなかった**（`membersFor` の「その月に実績・希望を持つ人」条項で全セル「—」の行がグリッド・印刷に残り、公開すると本人に通知も届いていた）
  - 削除の範囲は `userId` 一致かつ `date > leaveDate` のみ。**退職日当日までの希望・他人の希望・シフトは1件も触らない**（退職日を空欄に戻す保存でも何も消さない）
- **③ ロールの段変更でガード5（店舗未所属ユーザーの降格禁止）がすり抜けて画面が固まっていた**（`saveRole`）。`saveStaff` 側にしかガードが無く、**Lv5ロールを店舗未所属ユーザー（オーナー高橋誠）に付けたあと、そのロールの段だけを下げる**経路で `storeOf(undefined)` → `esc(st.name)` の TypeError が `render()` 内で throw され、以後どの操作も効かなくなっていた（リロードで復旧）。段変更時にも「変更後の段が `otherStores` 未満になり、そのロールを持つ店舗未所属ユーザーがいる」なら中止する判定を追加
- **④ 退職した元店長のレコードが永久に編集不能だった**（`saveStaff` の店長1店舗1人ガード）。後任を立てたあとは**退職者本人の氏名・退職日を直そうとしても必ず「店長はすでに△△さんです」で中止**され、逃げ道はロールをスタッフに書き換える（＝履歴を壊す）ことだけだった。判定に `&& !willRetired` を追加し、**保存対象者自身が退職者になる保存では重複判定をしない**ようにした（全参照が `!retired` で絞られており実害がないため）。在籍者どうしの重複・退職者を在籍に戻す保存では従来どおり止まる

### 修正（推奨・すべて反映）
- **⑤ 退職者に残ったシフトが店長グリッドと本人カレンダーで食い違っていた**。本人の「自分のシフト」タブ・「希望提出」タブが**セル関数の先頭で在籍外なら無条件に「—」**を返していたため、**本人だけが自分に残っているシフトを見られず、同じ本人が見る「店舗全体」タブでは見えている**状態だった。店長グリッドと同じ「**シフト（希望）があれば表示、無いときだけ『—』**」の分岐順に揃えた
- **⑥ 無効化した店舗に残る退職者がマスタ管理から消え、編集も異動もできなかった**。スタッフ一覧のグループ化が「店舗未所属＋有効店舗」だけだったため、**全員に退職日を入れて店舗を無効化すると5名が一覧から消え、復職・別店舗への異動が片道**になっていた。無効化店舗のグループを**末尾に薄表示**で追加した
  - あわせて、無効化店舗に所属する人の編集モーダルでは**現在の所属を「◯◯（無効化済み）」として選択肢に出す**（出さないと select の既定値に化けて、開いて保存しただけで無言の所属替えになるため）
- **⑦ 一度でも店舗を付けると二度と「（所属なし）」に戻せなかった**（`openStaffEdit`）。**編集時は常に「（所属なし）」を選択肢に出し**、可否の判断はガード5に任せる形にした（追加モーダルには従来どおり出さない＝所属店舗は必須）
  - なお「（所属なし）」の選択肢自体は**残す**（reviewer 判断）。無いと店舗未所属のオーナーを編集した瞬間に select の既定値＝先頭の有効店舗が保存され、**無言で本店へ所属替え**されてしまう
- **⑧ boot の視点防御で `view` を捨てたとき `saveView()` を呼んでいなかった**。無効な `ssd:view:v2` が端末に残り続けていた（実害なし）
- **⑨ 未参照になっていた旧 `members(storeId)` を削除**した。残しておくと将来これを使って「月・在籍の概念が抜けた実装」に戻るため、`membersFor(storeId, y, m)` を使う旨のコメントだけを残した

### 修正（オーナー確定・追加）
- **⑩ 公開通知の宛先から退職者を除外**した（`publish`）。②でシフトは実績として残す決定にしたため、**退職者に未来月のシフトが残っている状態でその月を公開すると本人に「シフト公開のお知らせ」が届く**経路が残っていた。「辞めた人に元の職場の公開通知が届くのは実運用として不自然」というオーナー判断により `!retired(u)` を追加（toast の「スタッフN名に通知」の人数も在籍者だけの数になる）。**グリッド・印刷には従来どおり残る**（掃除できるようにするため）

### 検証
- **自動生成の決定性・退行なし**: v1.5.2 のベースラインと**同日同条件**で再比較し、3店舗×来月の**全465件のハッシュが完全一致**（`e5e2f4b1c57abb5d`／ID込み・ID抜きとも・3回連続実行も一致）。①〜⑩はいずれも表示・保存・通知の分岐のみで、`generateForMonth` と `canAssign` には触れていない
- **reviewer の再現手順での実測**: ①来月生成→元店長に昨日付の退職日→後任を昇格 → **ダッシュボード・視点選択とも「店長: 田中優奈」で一致**／②退職日設定で来月の希望が消え、**幽霊行なし・公開通知なし・シフト件数は不変**／③段変更が toast で中止され、**その後もダッシュボード・マスタ管理が普通に開ける**（負のコントロール: ガードを迂回して state を直接壊すと同じ経路で実際に落ちることを確認済み）／④退職した元店長の氏名・退職日を保存できる／⑤本人カレンダーのシフト件数が店舗全体ビューと**一致**（14件＝14件）／⑥無効化店舗の5名が末尾に `opacity .55` で表示され、**復職・別店舗への異動ができる**／⑩退職者に通知0件・在籍者には届く
- **再実行した自動テスト**: jsdom **215項目**（v1.6.0 の210項目のうち、②で仕様が変わった4項目を新しい契約に書き替え、シフトを巻き込んでいないことの照合5項目を追加）＋ **reviewer 指摘の再現専用 76項目**（負のコントロール含む）＋ **実ブラウザ 29項目**（v1.6.0 と同一）＋ **実ブラウザでの再現手順 28項目**＝**すべて成功・console エラー0・横あふれ0**

### 既知の事項（v1.6.0 から更新）
- 退職日より後に**希望だけ**が残る問題は②で解消した（保存時に削除）。**シフトは実績として残す**方針のため、退職者の行は残シフトがある月のグリッド・印刷・CSVには引き続き出る（赤枠＋削除専用モーダルで掃除する）
- 店舗未所属ユーザーがいるロールは、そのロールの段を `otherStores`（Lv5）未満に下げられない（③のガード）。下げたい場合は先にそのユーザーへ所属店舗を設定する
- v1.6.0 の他の既知事項（転属者の所属履歴なし・月の公休日数を按分しない・既定では店長が時給を見られる）は据え置き

## [1.6.0] - 2026-08-13

クライアント自身が運用できる**マスタ管理（⑦）・在籍期間（⑧）・権限＝ロールの階層レベル制（⑨）**を追加した（実装指示書『追加実装指示書 第1弾』に基づく第1弾）。変更は `index.html` と `demo-data.js`（**ロール定義の追加のみ**。users/stores/conditions/patterns/requests は不変）。**データ構造が変わるため `STATE_KEY` を `ssd:state:v6` へ、`VIEW_KEY` を `ssd:view:v2` へ上げた**＝既存端末は開いた時点で自動的に再シードされる。アプリ内のバージョン表記は **v1.6**。

### 追加 — ⑦ マスタ管理（オーナーダッシュボード「🏬 マスタ管理」）
- **店舗**: 追加（名前・方式・色・使用パターン）／編集（名前・色・使用パターン）／**無効化（アーカイブ）と再開**
  - **方式（パターン制／自由時間制）は追加時のみ指定・変更不可**（`conditions` の形と既存シフトの整合が壊れるため）
  - 店舗追加時に **`state.conditions[id]` を必ず既定値で初期化**する（無いと自動生成・不足判定が空条件で動く）
  - **削除ではなく無効化**（`archived: true` のフラグのみ・データは一切消さない）。ダッシュボード本体から外れて「無効化した店舗」に薄表示で並び、タップで**過去実績の閲覧・印刷・CSVはできる**が自動生成・公開・条件設定のボタンは出ない
  - ガード: **在籍中のスタッフが1人でもいる店舗は無効化できない**（先に退職日の設定か所属変更を求める）／店舗名は無効化済みも含めて重複不可（編集時は自分自身を除外）／パターン制は使用パターン1つ以上必須
- **スタッフ**: 追加・編集（**氏名・所属店舗・ロール・入社日・退職日＝台帳系**）。時給・勤務条件は従来どおり「個人設定」側（役割分担を固定）
  - 新規スタッフは `wage` を持たない（＝扶養計算の対象外）・`ytdYen: 0`・`dependent` 未設定（既定ルール）・週上限/曜日なし
  - 一覧は有効店舗ごとにグループ表示（店舗未所属のオーナー級は先頭に別枠）。**退職者も表示**（「退職済み YYYY-MM-DD」バッジ＋薄表示）
- **ロール（権限の段）**: 追加（**入力は名前と段の2つだけ**）・編集・削除。段は Lv5→Lv1 の降順にキャプション付きで表示
  - 既定3ロールは**名前のみ変更可**（段の変更・削除は不可＝UIで disabled／削除ボタンを出さない）。追加ロールは**設定ユーザーが0人（退職者含む）のときだけ**削除可
- 保存はモーダル単位で即 state 反映（共通設定のドラフト方式ではない）。動的な文字列はJS引数に埋めずID渡しに統一

### 追加 — ⑧ 在籍期間（入社日・退職日）
- `users[].joinDate` / `leaveDate`（`"YYYY-MM-DD"`・**任意**・未設定＝制限なし）。**退職日は「最終在籍日」**（その日までは勤務可・翌日から在籍外）
- **在籍期間外の日は 自動生成・手動割当・希望提出のいずれも不可**
  - `canAssign()` の先頭で `activeOn()` を判定（**希望由来の割当てより前**）。判定は日付文字列の比較のみで**乱数・時刻に依存しない＝生成の決定性は維持**
  - `openCell()` は在籍外の日にモーダルを開かず toast。ただし**シフトが残っている在籍外セルだけは「削除専用モーダル」**を開く（退職日を後から入れた等の矛盾データを掃除する経路）。`_saveShift()` / `openReq()` / `_putReq()` にも同じ防御
- **在籍期間外のセルは「—」表記**（グレー淡色・印刷にも出す）。「割当のない日は必ず公休/有給で埋める」仕様の**唯一の例外**（在籍外の日を「公休」と出すと休みを与えているように見えるため）。店長グリッド・スタッフのカレンダー・希望提出タブ・店舗全体ビューで共通
- `members()` を**月対応の `membersFor(storeId, y, m)` に置き換え**（グリッド・印刷・扶養集計・公開通知・個人設定一覧・ダッシュボードの店長名）。「**その月に在籍していた人 ∪ その月にその店舗のシフト・希望データを持つ人**」＝退職日以降は一覧・印刷・CSV・通知から外れ、**在籍していた過去月では従来どおり表示される**
- 退職日より後にシフト・希望が残る保存は**止めずに警告 toast**を出し、該当セルは**赤枠＋シフト表示のまま**残す（掃除できるように）

### 追加 — ⑨ 権限（ロールの階層レベル制）
- `state.roles`（`{id, name, level}`）を新設。**上位の段は下位の権限をすべて含む**ため、逆転した権限（公開はできるが編集はできない等）は構造的に作れない
- 5段構成: **Lv1 スタッフ／Lv2 シフト編集／Lv3 シフト公開／Lv4 時給・扶養の閲覧／Lv5 マスタ管理・共通設定・他店舗閲覧**。既定は スタッフ=Lv1・**店長=Lv4**・オーナー=Lv5（Lv2・Lv3 は空の段＝副店長などを挿す場所）
- **view を `{ userId }` に一本化**し、表示する画面は `uiModeOf()` で権限から導出する（`VIEW_KEY` を v2 へ）。これにより「副店長＝シフト表は触れるが**公開ボタンが出ない**」という視点ごとの出し分けが成立する
- 出し分け: 公開ボタン=`publish`／扶養UI4系統（グリッドの⚠・ツールバーのチップ・セルモーダルの扶養行・個人設定の給与3項目＋計算ボックス）=`wage`／マスタ管理・共通設定=`master`／全店舗ダッシュボード=`otherStores`
  - `wage` 権限が無い視点で個人設定を保存しても、**時給・累計額・扶養区分は既存値のまま変更されない**（DOMのnullチェック）
- 視点選択画面: オーナー欄は `otherStores` を持つ在籍者を列挙／店長欄は有効店舗ごと（不在なら「店長: -」＋タップで案内 toast）／スタッフ欄には**追加ロール（副店長等）のユーザーも「氏名・店舗・雇用形態・ロール名」で出る**。未入社の人は出す（来月の希望提出のため）・退職者だけを除外
- ガード: **店長は1店舗1人**（新規追加・ロール変更に加え**所属店舗の変更経路**も保存後の状態で判定）／**ロックアウト防止**（マスタ管理ができる在籍ユーザーを0人にする変更を止める）／**店舗未所属ユーザーの降格禁止**（`storeOf(undefined)` で画面が落ちる経路を塞ぐ）／boot時も「ユーザー参照切れ・退職・所属店舗なし」を検知して視点選択に戻す

### 変更
- `STATE_KEY` `ssd:state:v5` → **`ssd:state:v6`**（`OLD_STATE_KEYS` に v5 を追加・`seedState` の `version: 6`・`loadState` の判定 `=== 6` を**4点セットで**変更）／`VIEW_KEY` `ssd:view:v1` → **`ssd:view:v2`**（boot で旧キーを削除）。後方互換の読み替えはしない（従来方針どおり再シード）
- 共通設定・個人設定一覧・オーナーダッシュボードの店舗列挙を `activeStores()`（無効化店舗を除外）に変更
- 設定モーダルのバージョン表記 v1.5 → **v1.6**

### 検証
- **自動生成の決定性・退行なし**: 実装前に v1.5.2 のコピーを**同日に**実行して採取したベースラインと比較し、3店舗×来月を生成した**全465件のハッシュが完全一致**（`e5e2f4b1c57abb5d`／ID込み・ID抜きとも）。3回連続実行も一致。**店長グリッドHTML・CSV出力・店舗全体ビュー・扶養の金額・人員不足バッジ列も v1.5.2 と一致**（差分は空白のみで最終的に0）
- **v5 state が残った端末**: 再シードされ `ssd:state:v6` のみ残る（`ssd:state:v1〜v5`・`ssd:view:v1` は削除・視点選択に戻る）
- **印刷（実ブラウザ・`preferCSSPageSize`）**: 月間シフト表=**A4横1枚**／個人分=**A4縦1枚**／店舗全体=**A4横1枚**／**全セルが在籍外表記「—」になる最悪ケースでもA4横1枚**（グリッドの行数・セル数・ヘッダ数は不変）
- **在籍期間**: 入社日=来月15日の新規スタッフ → 14日以前は割当0・グリッドは「—」14セル・CSVに行なし・希望提出は toast で停止／15日以降のみ割当・希望提出可。退職日=今日 → 当日は在籍扱い（セル編集・希望提出可）・翌日以降の月は一覧/CSV/通知から外れ・**在籍月（今月）は従来どおり表示**。`leaveDate < joinDate` は却下。退職日より後の残データは警告 toast＋赤枠＋削除専用モーダルで掃除できる
- **権限**: 副店長（Lv2）を作って付与 → 視点選択に「・副店長」で出る → manager モード（グリッド・セル編集・自動生成・条件設定あり）だが**公開ボタンなし・扶養UI4系統なし**・個人設定を保存しても時給が消えない。Lv3 は公開可・扶養は非表示（wage境界の上下を確認）。店長（Lv4）・オーナー（Lv5）は従来どおり
- **ガード**: 店長重複は**3経路すべて**で停止／唯一のマスタ管理者のロール変更・退職日設定を停止／Lv5をもう1人作っても**店舗未所属ユーザーの降格は停止**／所属店舗が消えた・退職した視点は**リロードで視点選択に戻る**（例外なし）
- **マスタ管理**: 店舗追加→`conditions` 既定値で初期化→スタッフ追加→自動生成が動く／パターン0のパターン制店舗・重複名・空文字は却下／方式は編集不可／在籍者がいる店舗の無効化を停止→退職日を入れると無効化可→ドリルインで実績閲覧・自動生成/公開/条件設定は非表示→再開で復帰／無効化店舗は視点選択・共通設定・スタッフ追加select・個人設定一覧に出ない
- **実ブラウザ（Chrome・`file://` 直開き・390px幅）でのクリック操作**: 視点選択→マスタ管理→店舗追加→スタッフ追加（`input type="date"`）→副店長ロール作成・付与→視点切替で公開ボタンが無いことを確認→新スタッフ視点で入社日前の希望提出が止まる→入社日以降は提出できる→デモデータにリセットで初期状態に戻る、まで通し。**横あふれ0・JSエラー0**
- 自動テスト計 **210項目（jsdom）＋29項目（実ブラウザ）が全て成功**・console エラー0

### 既知の事項
- **転属者は所属履歴を持たない**ため、新店舗の過去月にも「公休」行として出る（旧店舗の過去月は実績データ側の条項で表示される）。デモ運用で転属を多用しない前提
- 退職日より後に**希望（`requests`）だけ**が残っている場合、その月のグリッドに行が残り公開通知も届く（`membersFor` の「実績データを持つ人」条項による意図した表示）。**シフトは削除専用モーダルで消せるが、希望を店長が消す導線は無い**（保存時の警告 toast で整理を促す）
- 途中入社・途中退職の月でも**月の公休日数は按分しない**（第2弾のスコープ）
- 既定では店長（Lv4）が全員の時給・扶養を閲覧・編集できる。クライアントが Lv2/Lv3 にロールを作って付け替えることで解消できる

## [1.5.2] - 2026-08-11

v1.5.1（カレンダーのシフトチップ2行化）に対するreviewer検証で見つかった残件3件と、CHANGELOGの記載訂正2件。変更は `index.html` のみ（CSS 2ブロック＋表示用マークアップ2か所・`demo-data.js` と `STATE_KEY` は変更なし＝再シードは起きない）。**ロジックは一切触っていない**。アプリ内のバージョン表記は従来どおり major.minor のみなので **`v1.5` のまま据え置き**。

### 修正
- **① 希望提出タブのチップ省略15件を解消**（デモ台本の手順1で最初に見せる画面）。390px幅で「希望休」→「希…」、「早番希望」→「早番…」、「10:00-15:00」→「10:…」と省略されていた
  - `.chip.ghost` は**カレンダーの「公休」と共用**のため、グローバルに折り返し可にはせず、**希望提出タブ側だけに効く新クラス `.chip.cal-rq`** を追加（`display:block` ＋ `white-space:normal` ＋ `overflow-wrap:break-word` ＋ 左右padding 2px）
  - 時刻の途中や単語の途中で不自然に折れないよう、**時間指定は `10:00` / `15:00` の2要素**（`.cs-end`）、**パターン希望は `早番` / `希望` の2要素**（`.rq-sfx`）に分けて画面では2行に積む。「希望休」「終日可」は390px以上で**1行に収まる**
  - 時間指定チップだけ `.rq-tm` で `white-space:nowrap`（320px幅で「10:0 / 0」と割れるのを防ぐ）
  - 印刷側の詳細度対策: `.chip.cal-rq`（0,2,0）は `@media print` の `.chip`（0,1,0）より強いので、印刷側で `display:inline-block` / `padding:.3mm 1mm` / `overflow-wrap:normal` / `.cs-end`・`.rq-sfx` を `display:inline` に戻し、`.cs-end::before{content:"-"}` で**紙は従来どおり「10:00-15:00」「早番希望」の1行**にした
- **② 320px幅で時刻が「13:0 / 0」と4行に割れる回帰を修正**（v1.5.1で入れた `word-break:break-all` が原因。チップ高47.6px）。`.chip.cal-sh` から `white-space:normal` と `word-break:break-all` を外し、**`.chip` の `nowrap` を継いだまま** `.cs-end` の `display:block` だけで2行にする形へ。`break-all` は長いパターン名のために入れたものだが、パターン名は③で別クラスに分けたので不要になった
- **③ パターン制店舗（駅前店・モール店）のチップが不要に小さくなった回帰を修正**。v1.5.1では `.cal-sh` がパターン名チップにも付いてしまい font-size 10px→9.5px・高さ17.5px→13.4px・shrink-to-fit→全幅ブロックに変わり、**同じマス内の「公休」より薄く小さいという逆転**が起きていた
  - パターン名チップを**新クラス `.chip.cal-pt`** に分離。**見た目は従来の `.chip` のまま**（10px・font-weight 700・inline-block・padding 1px 5px）で、`white-space:normal` ＋ `overflow-wrap:break-word` により**長いパターン名の折り返しだけ**を許可する（`break-word` は min-content 幅に影響しないので、収まる名前のチップ幅は従来と同一）
  - 320px幅（iPhone SE 1st相当）だけはマス内寸が狭く2文字でも1行に入らないため、`@media(max-width:340px)` で左右paddingのみ 5px→2px に詰めた（360px以上には一切影響しない）
- **④ CHANGELOG（v1.5.1）の記載訂正2件**（reviewer指摘）
  - シフトチップの省略「140件 → 0件」→ **「141件 → 0件」**（内訳: 今月72件＋来月69件。来月ぶんは3店舗を自動生成＋公開した状態での実測）
  - 印刷の「個人分も紙面寸法が一致」→ チップ**幅のみ 0.03px以内の差**（65.08→65.11px / 65.31→65.33px）。高さ・マス・カレンダー全高・本文全高は一致

### 検証（Chrome headless・`file://` 直開き・CDP／HEAD・v1.5.1・本修正の3系統を同一手順で比較）
- **①: 390pxで希望提出タブの全チップを機械判定**（`scrollWidth - clientWidth` と Range で測った実テキスト幅の両方）→ 省略・はみ出しとも **15件 → 0件**。「希望休」「終日可」は1行（高さ17.5px）、「早番希望」「10:00/15:00」は2行（33px）。**公休チップは HEAD・v1.5.1 の両方と 320/360/375/390/414/430px の全幅で寸法・テキストとも完全一致（各186件）**、有給も全一致（各6件）
- **②: 320/360/375/390/414/430px の全幅**でカレンダーのシフトチップの高さを機械判定 → 自由時間制チップは**全幅で 24.78px（2行）で一定**（320pxは v1.5.1 の 47.56px から回復）、パターン制チップは**全幅で 17.5px（1行）で一定**。マスの縦クリップ0・ページ横あふれ0
- **③: パターン制チップの計測値が 360/375/390/414/430px の全幅で HEAD と完全一致**（各114件・高さ17.5px・幅30px・font-size 10px・font-weight 700・inline-block・padding-left 5px）。320pxのみ padding が 5px→2px（意図的。HEAD ではここが「早…」と省略されていた）。長いパターン名は省略されず折り返す
- **印刷**: 個人分（自由時間制／パターン制とも）= **A4縦1枚**（MediaBox 594.96×841.92pt）／店舗全体 = **A4横1枚**（841.92×594.96pt）／店長の月間シフト表 = A4横1枚。印刷メディアでの寸法は**v1.5.1と完全一致**（自由時間制チップ 幅65.11/65.33px・高さ20.78px／パターン制チップ 幅63.11・61.73px・高さ36.28px／希望提出タブのチップ 幅41.52px・高さ20.78px＝HEADと同値／マス79.36px・カレンダー515.8px・本文571.83px）。PDFのバイト長も4種すべてv1.5.1と同一
- **画面/印刷の出し分けを両方向で確認**: 画面には印刷用スタイルが出ない（希望提出タブのチップは画面で font-size 10px・padding 2px・block）／紙には画面用の2行化・全幅化が出ない（印刷では inline-block・padding 1mm・`.cs-end`/`.rq-sfx` は inline、時間指定は `-` 区切りの1行、パターン希望は「早番希望」の1行）
- **自動生成の決定性**: 3店舗の来月を生成した全465件のMD5が HEAD・v1.5.1・本修正で一致。店長グリッドHTML・CSV出力のMD5も一致
- **動作確認**: 希望提出タブで日付タップ→モーダル起動→「😴 希望休」提出まで実行し、提出済み1件→2件・カレンダーに `chip ghost cal-rq`（高さ17.5px＝1行）が描画・トースト「店長に送信されました」を確認
- **console エラー0**（3系統とも）

### 既知の残り（今回対象外・HEADから存在）
- 320px幅（iPhone SE 1st相当）では「公休」が5px・「有給」が7px 省略される（`.chip.ghost` / `.chip.leave` は今回触っていないため HEAD と同一）。自由時間制のシフトチップと時間指定の希望チップも同幅でそれぞれ 約1px・約2px はみ出す（HEADでは35px省略だったので大幅に改善）。商談で使う390px以上では0件

## [1.5.1] - 2026-08-11

スタッフ視点「自分のシフト」カレンダーで、スマホ幅だとシフトの時刻が「13:...」と省略され何時から何時か読めなかったのを修正した（オーナーがiPhone実機で発見）。変更は `index.html` のみ（CSS 2か所＋表示用マークアップ1か所・`demo-data.js` と `STATE_KEY` は変更なし＝再シードは起きない）。**ロジックは一切触っていない**。アプリ内のバージョン表記は従来どおり major.minor のみ（v1.0/v1.4/v1.5）なので **`v1.5` のまま据え置き**（v1.4.1・v1.4.2 のときと同じ扱い）。

### 修正
- **カレンダーのシフトチップを2行表示に**（1行目=開始時刻／2行目=終了時刻）。店長の月間シフト表・スタッフの「店舗全体」で実績のある `.sh-chip` と同じ見せ方に揃えた
  - 原因: `.chip` の `white-space:nowrap` + `overflow:hidden` + `text-overflow:ellipsis`。iPhone（幅390px）ではカレンダー1マスの内寸が約35pxしかなく「13:00-19:00」（約60px必要）が入り切らず省略されていた
  - **`.chip` をグローバルに変えず、新クラス `.chip.cal-sh` を追加してカレンダーのシフトチップだけに効かせた**。公休（`.chip.ghost`）・有給（`.chip.leave`）・希望提出タブのチップは見た目・DOMとも従来のまま
  - 自由時間制の店舗: `<span class="chip cal-sh">10:00<small class="cs-end">19:00</small></span>` の2要素構成にし、画面では `.cs-end` を `display:block`（2行）、印刷では `display:inline` ＋ `::before{content:"-"}` で**従来どおり「10:00-19:00」の1行**に戻す
  - パターン制の店舗: パターン名（「早番」等）は従来どおり1行だが、`white-space:normal` + `word-break:break-all` にしたので長い名前でも省略されず折り返す。印刷用の `.pr-time`（時刻併記）は**画面に露出しない**まま（`.cs-end` を別クラスにして `.chip .pr-time{display:none}` と競合させていない）
  - 印刷側の詳細度対策: `.chip.cal-sh`（0,2,0）は `@media print` の `.chip`（0,1,0）より強いため、印刷側でも `display:inline-block` / `font-size:8pt` / `padding:.3mm 1mm` / `line-height:inherit` / `word-break:normal` を明示して**画面用の2行化が紙に漏れない**ようにした

### 検証（Chrome headless・`file://` 直開き・CDP／修正前後を同一手順で比較）
- **390px（iPhone相当・mobile emulation）で全12スタッフ×今月/来月のカレンダー全セルを機械判定**（`scrollWidth - clientWidth` とマス枠からのはみ出し）: シフトチップの省略・はみ出しは **修正前 141件 → 修正後 0件**（内訳: 今月72件＋来月69件。自由時間制の本店が対象。パターン制の駅前店・モール店は修正前から0件）。マスの縦方向クリップ（`.day` の `scrollHeight > clientHeight`）は**全0件**、ページ自体の横あふれも0
- **公休・有給・希望提出タブは修正前と完全に同一**（希望提出タブのチップ計測値は修正前後で1件ずつ一致。※希望提出タブには**修正前から**「10:00-15:00」「早番希望」「希望休」等が数px〜25px省略される同種の症状が15件あるが、今回の指示範囲外のため触っていない → 別途の判断待ち）
- **印刷は変化なし**: 個人分 = **A4縦1枚**（MediaBox 594.96×841.92pt）／店舗全体 = **A4横1枚**（841.92×594.96pt）／店長の月間シフト表 = A4横1枚。店舗全体と店長のPDFは修正前と**バイト単位で完全一致**。個人分もA4幅で印刷メディア描画したときのチップ高さ20.78px・マス高さ79.36px・カレンダー全高515.8px・本文全高571.83pxが**修正前と一致**し、紙面は「13:00-19:00」の1行のまま。※チップ**幅**のみ厳密には一致せず **0.03px以内の差**（65.08→65.11px / 65.31→65.33px。`::before{content:"-"}` で区切りを描くため）
- **自動生成に変化なし**: 3店舗の来月を生成した後の全シフト465件のMD5が修正前後で一致（`edc2a4b136fe5e0177e2cfa5c943b715`）。店長グリッドのHTMLとCSV出力のMD5も一致
- **console エラー0**（修正前・修正後とも）

## [1.5.0] - 2026-08-11

スタッフ画面から「店舗全体のシフト」を月単位で見渡せるようにし、その印刷を追加した。変更は `index.html` のみ（`demo-data.js` は検証済みの扶養シナリオを壊さないため変更なし・`STATE_KEY` も `ssd:state:v5` のまま据え置き＝再シードは起きない）。

### 追加
- **シフトタブ内のセグメント切替「自分のシフト / 店舗全体」**
  - 下部タブは3つ（シフト／希望提出／通知）のまま増やしていない。切替はタブ内のセグメントで行う
  - 切替状態は `ui.staffShiftView`（`"mine"` / `"store"`）に**メモリ上でのみ**保持する。localStorage には持たないので**リロードすると「自分のシフト」に戻る**（保存データの構造を変えないため）
  - 「自分のシフト」＝従来の月カレンダー。表示・印刷とも一切変更していない
  - 月送りは既存の `A.staffCal()` を共用するので、切替の前後で表示中の月が変わらない
- **「店舗全体」ビュー（読み取り専用の月間シフト表）**
  - 新しい描画は作らず、店長の月間グリッド `managerHtml()` に**第3引数 `ro`（`{ meId, monthOffset }`）**を渡して再利用している。A4横1枚に収まることを実測済みのレイアウトを作り直さないため
  - `ro` のときの差分は次の6点のみ:
    1. **公開済み（`status === "published"`）のシフトだけを描く**。下書きはセル単位で落とす（公開済みの月に店長が後から入れた下書きも出さない）
    2. **未公開の月は表そのものを描かない**（案内文だけ。印刷見出しも印刷ボタンも出さない＝下書きが紙に出る経路を作らない）
    3. セルに `onclick` を付けない（タップしても編集モーダルは開かない・`cursor` も既定に戻す）
    4. **給与に関わる情報を出さない**（扶養超過見込みの `.fw` / `.fw-chip`、スタッフ名の「⚙ 個人設定」と個人設定モーダルへの導線）
    5. 店長向けの指標を出さない（人員不足バッジの `tfoot`、希望と食い違う日の赤枠、公休の由来表示「希望 / 希望未反映」）
    6. **自分の行をハイライト**（淡い青の地色＋名前の横に「あなた」バッジ）
  - 他店舗は出さない（所属店舗のみ）。公休・有給の表示は店長画面と同じ扱い
- **「🖨 全体を印刷」（A4横1枚）**
  - 用紙の向きは既存の `setPageSize()` がビューに追従する（**店舗全体＝A4横 / 自分のシフト・希望提出・通知＝A4縦**）
  - 印刷見出しは「**店舗名／対象月／出力日時／公開済みのシフトのみ記載**」。下書きは含まれないので「下書き」表記は出さない
  - 既存の「🖨 このシフトを印刷」（個人分・A4縦）はそのまま残している
  - 給与・扶養の情報が紙に出ない扱い（`.fw` `.fw-chip` が `display:none`）は維持。加えて `ro` では画面にも出さない

### 検証（Chrome headless・`file://` 直開き・CDPで実測）
- **下書きがスタッフに漏れないこと**
  - 来月を自動生成して**下書き90件がある状態**でスタッフの「店舗全体」を開く → `table.grid` 0個・`td.cell` 0個・チップ0個・**本文に `HH:MM` の文字列なし**。「◯年◯月のシフトはまだ公開されていません。」の案内のみ（印刷見出し0・印刷ボタンなし）
  - **公開済みの月に下書きセルを1件混ぜる**（田中優奈の8/15を下書きに差し替え）→ `.sh-chip.draft` **0件**、該当セルは「公休」表示（公開済みの状態だけが見える）
  - 公開後（`A.publish`）は90件が正しく表示され、有給（山本典子9/18）も店長画面と同じ「有給」チップで出る
- **印刷がA4横1枚に収まること**: `Page.printToPDF`（`preferCSSPageSize`）で**1ページ・MediaBox `841.92 × 594.96 pt`（＝A4横）**。31日の月（8月）で**全155セルが公休の最悪ケースでも1ページ**（30日の月も同様）。印刷メディアをエミュレートした実測でも表の高さは 222px / 有効高 718px
- **「自分のシフト」はA4縦のまま**: `@page` は `A4 portrait`、PDFは**1ページ・MediaBox `594.96 × 841.92 pt`**。希望提出・通知タブもA4縦。「店舗全体」→「自分のシフト」に戻すと縦に戻り、リロード後も縦
- **390px幅での操作可否**: 表は `.grid-wrap` 内で横スクロール（表1522px・可視幅360px＝**約6日分**（実測5.74列）・スタッフ名列は `position:sticky` で常時表示）。**ページ自体は横にあふれない**（`documentElement.scrollWidth ≤ innerWidth`）ので縦スクロールと干渉しない。月末までスクロールしても名前列は残る
- **自動生成の決定性**: 全店・来月を3回連続生成して**完全一致**（署名ハッシュ一致・226件）
- **既存機能の退行なし**: v1.4.2（`d608acf`）と新版を同条件で並走させ、**店長グリッドのHTML（3店舗 × 今月/来月の6通り）・CSV（同6通り）・扶養計算（全16名 × 2か月の32通り）・人員不足バッジ（3店舗の日別ビット列）がすべて完全一致**。店長の印刷（下書きあり・A4横）も1ページのまま。console エラー0

### ドキュメントのみの修正（2026-08-11 追記・`index.html` / `demo-data.js` は変更なし）

バージョンは **1.5.0 のまま据え置く**（コード変更が無く、設定モーダルのバージョン表記＝アプリ内の v1.5 とずれないようにするため）。

- **README のデモ台本: 「下書きが漏れない」の見せ方を差し替え**
  - 旧手順9「前月に戻すと『まだ公開されていません』と出る」を**削除**した。前月には下書きが1件も無いため"漏れない証明"になっておらず、直前に月送りを触っていると成立せず、過去月なのに「公開されると通知でお知らせします」と出て突っ込まれる余地があった
  - 代わりに、**公開の直前（手順7）にスタッフ視点 →「店舗全体」→「翌月 ›」を挟む**構成にした。店長の手元に下書き90件がある状態で、スタッフ側は表0・案内文のみ・印刷ボタンなしを見せ、**そのまま公開 →（手順10）同じ画面を再表示 → 表が出る**というbefore/afterにしている
  - 手順は通しで振り直して 全12手順（旧7〜11 → 新8〜12）
  - 実測（Chrome headless・`file://` 直開き・CDP）: 本店の来月を自動生成 → **下書き90件** → スタッフ（田中 優奈）の「店舗全体」は初期表示が今月（公開済み・表1・155セル）、**「翌月 ›」1回で表0・`td.cell` 0・チップ0・本文に `HH:MM` なし・印刷見出し0・「全体を印刷」ボタンなし**、案内文は「2026年9月のシフトはまだ公開されていません。公開されると通知でお知らせします。」。公開後に同じ画面を出すと**表1・150セル・シフトチップ90・`.sh-chip.draft` 0・有給1・「あなた」行1（田中 優奈）・扶養表示0・個人設定リンク0・セルの `onclick` 0**。用紙向きも 店舗全体＝`A4 landscape` / 自分のシフト＝`A4 portrait` のまま。console エラー0
  - 「店舗全体」への切替は表示中の月を持ち越す（`ui.staffCalOffset` 共用）ことも実測で確認し、台本では手順10で月送りを繰り返さない書き方にした
- **README に想定問答を2件追加**（「商談での想定問答」節を新設）
  - 同僚の**有給が見える**件（紙のシフト表に有給を載せる運用が一般的なため仕様として妥当。出し分けは設定で対応可能／時給・扶養警告・年初累計額は「店舗全体」に一切出ない）
  - **スマホで月全体が見えない**件（一覧が欲しい人＝紙・スマホ＝自分の周辺確認、という役割分担。同じ画面の「🖨 全体を印刷」でA4横1枚が出せるので、その場で押して見せるのが最短）
- **CHANGELOG の訂正**: 390px幅の記述「一度に6日ぶん」→「**約6日分**（実測5.74列）」

## [1.4.2] - 2026-08-11

2026-08-13の商談MTG前 最終修正（第2弾）。reviewer指摘のうち4件のみを最小差分で対応（`demo-data.js` は検証済みの扶養シナリオを壊さないため変更していない）。変更は index.html の6か所のみ。

### 修正
- **要修正1: `STATE_KEY` を上げ忘れていた**（最優先）
  - v1.4.1 で生成結果（自由時間制の閉店スナップ）が変わったのに `ssd:state:v4` のままだったため、**v1.4.0 を一度でも開いた端末では旧stateがそのまま採用され**、シード済みの今月分が `10:00-18:45` のまま残っていた。そこへ v1.4.1 の `to-1` 判定が効くので**公開済みの今月に人員不足バッジが出るのに、自動生成ボタンは来月にしか無いため復旧できない**（エラーも出ないので気づけない）
  - `STATE_KEY` を **`ssd:state:v5`** に上げ、`OLD_STATE_KEYS` に `ssd:state:v4` を追加、`seedState()` の `version` と `loadState()` の判定も 5 に合わせた
  - 実測（基準日2026-08-13・v1.4.0で作った v4 state を残した状態で開く）: 修正前は `version=4` のまま採用され **本店8月（公開済み）4日・9月8日の⚠不足** → 修正後は **v5で再シードされ 8月0日・9月は自動生成後1日**（`ssd:state:v4` はキーごと削除される）
- **要修正2: 有給の日の賃金が二重計上される**
  - `monthEarnOf()` がシフト分と有給分を無条件に足しており、有給の日のセルに「この時刻で保存」をすると同じ日が2回計上されていた（`_saveShift()` は同日の `paid_leave` を消さない）
  - 有給分の集計に**「同じ日にシフトが無いこと」**を条件として追加した（有給側の挙動・通知は変更していない）
  - 実測（山本 典子・9月）: 自動生成後 118,750円 → 9/18（有給）に「この時刻で保存」で **修正前 125,000円 → 修正後 118,750円**。パターン「遅め」をあてはめた場合は **127,813円 → 121,563円**（シフト分9,063円のみ計上）
- **要修正3: 公休セルを開くと終了時刻が「06:00」になり保存できない**
  - 社員/店長の既定は拘束8時間45分＝`18:45` だが、時刻セレクトは30分刻みなので一致する選択肢が無く、**先頭の06:00が表示され**そのまま保存すると必ず「終了時刻は開始時刻より後にしてください」で失敗していた（「セルをタップして手動編集できます」が見せられない状態）
  - 既定の終了時刻を**30分単位に切り上げ**て、必ず選択肢に存在する値にした
  - 実測（社員 田中優奈の公休セル）: **`10:00〜06:00`・保存不可 → `10:00〜19:00`・保存成功（実働8時間・休憩60分）**。パート15:00・学生14:00・既存シフトのセル（現在の時刻）は従来どおり。既定実働7.5時間に変えた場合も `18:30`（保存成功・実働7時間45分）
- **要修正4: 段階制ルールの1段目を長い休憩にすると実働がマイナスになる**
  - `MAX_BREAK_MIN`（240分）以内でも、**短い段のほうが休憩が長い**設定は保存できてしまい、拘束から実働を逆算する側（`spanForWorkTiers`）と休憩を決める側（`breakByTiers`）が食い違って極端に短い実働のコマができていた
  - 保存時の検証に **①休憩は拘束が長い段ほど長い（または同じ）** **②段階制ルール変更後もパターンの実働が0以下にならない** の2点を追加し、パターン一覧の実働表示を**0でクランプ**した
  - 実測（1段目の休憩を240分）: 修正前は**そのまま保存でき**、拘束2時間のパターンが **「実働-2時間」**表示・本店9月の自動生成で**実働45分のコマ87件中28件** → 修正後は **「休憩ルールの休憩は、拘束が長い段ほど長く（または同じに）してください」で保存されず**、一覧表示も「実働0時間」（生成は実働最小240分・45分のコマ0件）。240分/240分/240分のように単調な設定はこれまでどおり保存でき、拘束4時間の自動パターンがあるときだけ「パターン「短番」は休憩が拘束時間以上になります（実働が0以下）」で止まる

### 対応していないもの（商談後に判断）
- 推奨3（開始時刻のスナップで実働が最大29分増えうる。`empField` のstepが0.5hのため実質到達しない）／推奨4（`tierField` の負値が無言で無視される）／推奨5（`breakMin: null`・文字列を通す。UI経由では到達しない）／軽微3件
- 自由時間制で「時間帯別 必要人数」を全部削除した場合の生成（`10:00＋拘束`）は30分刻みに乗らないことがある。この経路のセルを開くと同じく先頭値が出るが、デモ手順には含まれない

### 検証（基準日を2026-08-13に偽装して実測）
- 扶養シナリオ: **生成前は超過0名**（山本の残額28,750円・木村206,600円）→ **生成後は山本 典子のみ超過**（見込118,750円・−83,750円）、**木村 由美は残79,200円**。v1.4.1と完全に同値
- 自動生成の決定性: 全店・来月を3回連続生成して**完全一致（226件）**。同じ月の再生成でも件数不変（90→90）
- 退行確認: 全465件（8月公開済み＋9月下書き・全3店舗）のシフトのハッシュが v1.4.1 と**一致**（実働分布・CSV1行目も一致）。オーナー／店長／スタッフ各画面の描画サイズ、公開・個人設定・共通設定・リセットまで通してエラー0
- 実ブラウザ（Chrome headless・`file://` 直開き）でも 起動→店長→自動生成→公休セル保存（19:00）→有給セル保存（118,750円のまま）→共通設定の保存拒否→旧v4 stateからの再シード（8月0日・9月1日）を確認。console エラー0

## [1.4.1] - 2026-08-11

2026-08-13の商談MTG前 最終修正。reviewer指摘のうち4件のみを最小差分で対応（扶養デモデータは検証済みシナリオを壊さないため変更していない）。

### 修正
- **要修正2: 休憩の段階制ルールで「最終段」の休憩分が検証されていなかった**（最優先）
  - `saveCommonSettings()` の検証ループが `if(t.spanMaxMin != null)` で囲まれており、**最終段（`spanMaxMin === null`）が丸ごと素通り**していた。`A.tierField()` も休憩分に上限がなく（`max="240"` はHTML属性だけでJSは見ていなかった）、`9999` を入れて保存できてしまった
  - 休憩分の検証（**0〜240分**・`MAX_BREAK_MIN`）を**全段共通**に移し、入力時（`tierField`）でも弾くようにした
  - 実測（2段目を拘束7時間0分・3段目の休憩に9999）: 修正前はそのまま保存でき、パターン「遅め」が `休憩9999分・実働-159時間-39分`／社員の既定が `拘束174時間39分`／本店の自動生成で**実働0分のコマが90件中7件** → 修正後は**入力時に「休憩は240分までです」で弾かれ、保存経路でも「休憩ルール3段目の休憩は0〜240分で入力してください」で保存されない**（240分は保存できる／2段目以前の従来チェックは変更なし）
- **要修正1: 本店で閉店15分前（18:45〜19:00）の人員不足が検出されなかった**
  - (b) 生成側: 自由時間制の割当てで**閉店まで30分未満しか残らない場合は終了時刻を閉店にスナップ**するようにした。社員/店長が `10:00-18:45`（拘束8h45m・休憩45分）→ `10:00-19:00`（拘束9h・自動休憩60分）に戻り、**実働8時間は不変**
  - (a) 判定側: `isShortDay()` の30分刻みサンプリングは最終サンプルが18:30で `18:45〜19:00` を見ていなかった。**時間帯の末尾直前（`to-1`）も必ずサンプルする**ようにした
  - 実測（分単位で全域を走査した「真の不足」）: 本店 **9月 8日/30日・8月（公開済み）4日/31日 → 9月 1日・8月 0日**。残る 9/22 は 18:45 の件ではなく**15:00 からの不足**で、5連勤上限・希望休・出勤可能曜日により候補者が居ない日（修正前から出ていたもので、今回の変更による増加ではない）
  - 副作用の説明: 共通設定の雇用形態行（拘束8時間45分）と本店グリッドの実時刻（10:00-19:00）が食い違って見えるため、**「実際の時刻は店舗の営業時間に合わせて調整されることがあります」**の補足を1行追記した
- **推奨3: 有給の日の賃金が扶養見込みに含まれていなかった**
  - `monthEarnOf()` が `state.shifts` だけを集計しており、`requests` の `paid_leave` として持つ有給が賃金0扱いになっていた
  - **有給の日は「雇用形態の既定実働時間 × 時給」で見込みに加算**するようにした（有給の日は時刻が決まっていないため既定実働で見込む）
  - 実測（基準日2026-08-13）: 山本 典子の9/18の有給ぶん **+6,250円**（5h×1250）。生成後の見込み 112,500円 → **118,750円**、超過額 −77,500円 → **−83,750円**
- **推奨4: 手動休憩0分で「実働12時間・休憩0分」が無警告で作れた**
  - パターン保存時、**手動休憩が段階制ルールの求める休憩を下回る場合に警告を出す**ようにした。明示の上書きなので**保存自体は許可**する（従来の「休憩が拘束時間以上」は引き続き保存不可）
  - 実測: `09:00-21:00・手動0分` → 保存はできるが **「⚠ 休憩0分は自動ルール（この拘束時間なら60分）を下回ります。労働基準法の休憩を確認してください」** を表示。手動30分でも警告、手動60分/90分・自動・拘束5時間で手動0分は警告なし

### 対応していないもの（商談後に判断）
- 推奨1（年またぎの扶養リセット）: 8/13時点では8月→9月のため発生しない
- 推奨2（扶養デモデータが8月以外で成立しない）: 8/13は正常に再現することを確認済み。デモデータ変更は検証済みシナリオを壊すリスクがあるため見送り
- 推奨5（扶養上限が全社1本）／推奨6（店長が時給を閲覧・編集できる）／軽微6件

### 検証（基準日を2026-08-13に偽装して実測）
- 扶養シナリオ: **生成前は超過0名**（山本の残額28,750円）→ **生成後は山本 典子のみ超過**（−83,750円）。木村 由美は**残79,200円**で従来どおり
- 自動生成の決定性: 全店・来月を3回連続生成して**完全一致**（226件）
- 修正前後の生成結果の突き合わせ（全465件・8月公開済み＋9月下書き・全3店舗）: 差分は **`10:00-18:45（休45/実480）` → `10:00-19:00（休60/実480）` が11件のみ**。新規/消滅したコマは0件、全員の月間実働合計も一致。基準日を 8/11・9/1・12/15 に変えても同じ傾向（8件・6件、いずれも実働不変）
- 実DOM（jsdom）で index.html を読み込み、ログイン選択→店長グリッド→自動生成→印刷ビュー→CSV→オーナー共通設定→スタッフ画面→リセットまで通してエラー0。CSVは `本店（サロン）,2026-09-01,佐藤 美咲,10:00,19:00,,60,8.00`

## [1.4.0] - 2026-08-10

2026-08-13の商談MTG向け 第3波（最終）。休憩ルールの一本化・レビュー指摘の修正・扶養計算・個人設定。

### 変更（休憩ルールの一本化）
- **休憩の決まり方を2層に統一**した。①パターンに休憩が設定されていればそれを使う ②無ければ**段階制ルール**で自動的に決まる
  - **「雇用形態ごとの既定休憩」は廃止**（3層になると破綻するため）。雇用形態が持つのは**実働時間だけ**になった
  - `settings.employmentDefaults` から `breakMin` を削除。拘束時間＝実働＋段階制ルールの休憩で逆算する
- **段階制ルール**（`settings.breakTiers`）を追加。初期値は `拘束6時間以下→0分` / `拘束8時間45分以下→45分` / `それ超→60分`
  - 労基法34条（実働6時間超で45分・8時間超で60分）を拘束時間に置き換えたもの。実働8時間＋休憩45分＝拘束8時間45分が境目
  - **閾値・分数はすべて共通設定で変更可能**（法令の数字をハードコードしない方針）
- これに伴い**社員/店長の既定が 拘束9時間（実働8h＋休憩60分）→ 拘束8時間45分（実働8h＋休憩45分）** に変わった。実働は8時間のまま
- デモデータのパターンは 早番/遅番/中番/夜番＝**手動60分**、遅め＝**自動**（拘束8時間 → 45分）にして、2層の両方を商談で見せられるようにした

### 修正（reviewer指摘）
- **要修正A: 自動生成が実働3時間のコマをつくる**
  - 原因は ①下限 `Math.min(240, span)` が**拘束**4時間の下限だったこと ②`breakMinOf` が拘束の長さに関係なく既定休憩60分を返していたこと
  - 段階制ルール＋**実働4時間ぶんの拘束を下限**にする実装へ変更。前に伸ばせないときは後ろに伸ばす
  - 実測（本店・自動生成）: 実働3hのコマ **9月 7件 / 10月 10件 / 11月 6件 / 12月 9件 → すべて 0件**。実働分布も `3h:7,4h:23,5h:53,8h:7` → `4h:24,5h:37,6h:22,8h:7` に改善
- **要修正B: 雇用形態の既定がパターン制の店舗では効かない**（仕様どおりなので挙動は変えず説明を追記）
  - 共通設定の説明文に「自由時間制の店舗の自動生成に使います。パターン制の店舗はパターンの時刻が優先されます」を追記。READMEのデモ手順も**本店（自由時間制）で見せる**形に統一
- **推奨1: `breakMin=0` と「未設定」の区別がUIから作れない**
  - パターン編集の休憩欄を「**自動（ルールで決める）／手動◯分**」のトグルに変更。自動＝`null`（段階制）、手動＝入力値をそのまま使う（**0分も明示指定できる**）
- **推奨3: `patternRatio` が正規化されない**
  - 割合を**合計100%に正規化**してから使うようにした（割合未設定のパターンは均等割りを仮の目標にする）
  - 実測（駅前店にパターンを1つ追加）: `早番39.7% / 遅番38.2% / 遅め22.1%` → `早番38.2% / 遅番36.8% / 遅め25.0%`（実効目標 37.5/37.5/25.0）。パターン削除後も残りが100%になる
  - 条件設定の各行に**「実効◯%」**と合計・注記を表示（商談で説明できる形にした）
- **推奨4: 実働時間に上限バリデーションがない**
  - 上限を**16時間**に設定。`max`属性だけでなく入力時・保存時にも弾く。加えて自動生成側も終了時刻を24:00で丸める
  - 実測: 実働30時間 → 以前は保存でき `10:00-41:00` のシフトが14件生成されグリッド/CSVにも出た（本店の時間帯別必要人数が空のとき通る経路）→ **保存できず0件**。16時間で保存しても24時超えのシフトは0件
- **推奨5: パターン削除の確認文言**
  - 「**公開済み◯件を含む**」「スタッフのカレンダーの表示がパターン名から時刻に変わる」「**元に戻せません**」を確認画面に追記
- **推奨2/6/7: 説明文とREADMEの整合**
  - 推奨2: 共通設定「店舗ごとの使用パターン」に「**自由時間制の店舗では手動あてはめ用**（自動生成には使われない）」を追記
  - 推奨6: 「パターンの時刻を編集しても既存シフトの時刻は変わらない（CSVでも名前と時刻が食い違うことがある／作り直すには自動生成をやり直す）」をREADMEに明記
  - 推奨7: 「自由時間制の店舗はパターン名が空欄」という不正確な記述を、「パターンをあてはめていないシフトが空欄」に修正

### 追加
- **CSVに「休憩(分)」「実働時間(h)」の2列を追加**。既存の列順は変えず末尾に追加（BOM・CRLF・インジェクション対策・`_下書き` 付与は現行どおり）
  - 実働時間は `終了−開始−休憩` を時間の小数（例 `7.25`）で出力。**Excel側でも時給を掛けるだけで同じ扶養計算ができる**
- **個人設定**（新規画面）。店長のシフト表で**スタッフ名をタップ**、またはオーナーの「👤 個人設定」から開く
  - 雇用形態／週の最大勤務日数／出勤可能曜日（従来は demo-data.js 固定で編集不可だった）＋**時給／年初〜先月の累計額／扶養内・扶養外の区分**
  - 既定は 社員・店長＝扶養外、パート・学生＝扶養内
  - **休憩は個人設定に持たせない**（2層を維持するため）
  - 動的な文字列をJS引数に埋め込まず**ID／インデックス渡しに統一**（`A.empField('${esc(t)}',…)` も index 渡しに修正。名前に `'` が入っても壊れない）
- **扶養範囲内の計算（仮版）**。入力値は仮・**計算は本物**
  - 見込み額 ＝ その月のシフトの**実働時間 × 時給**。`累計 ＋ 見込み` と上限額を比べて残額・超過を出す
  - **上限額は共通設定の入力欄**（初期値123万円）。基準額の改正に追従できるようにするため、金額をハードコードしていない
  - **扶養外の人は計算・警告の対象外**
  - 警告バッジ: 店長グリッド（スタッフ名の下＋ツールバーの「⚠ 扶養超過見込み ◯名」）／オーナーダッシュボード（店舗ごとの人数と対象者名）／セル編集・個人設定に内訳
  - 給与情報のため**印刷では非表示**
  - **意図的にスコープ外**: 扶養は自動生成の制約に入れていない（表示・警告のみ）。乱数を使わない決定的実装の維持を優先した
- デモデータにパート・学生の**時給**と**年初からの月平均支給額**を追加。累計額は「今日」基準で `月平均 × 年初〜先月の月数` として生成する（固定額を焼き込まないため）

### 内部
- localStorage キーを `ssd:state:v4` に更新（settings構造の変更・users への項目追加のため再シード）。旧 v1/v2/v3 は起動時に削除
- `patternTargets()`（割合の正規化）・`breakByTiers()` / `spanForWorkTiers()`（段階制ルール）・`fuyouOf()`（扶養計算）を追加

### 検証
- ロジック回帰テスト **80項目すべてOK**（決定性・休憩の境界値・上限バリデーション・割合の正規化・CSV・扶養・全画面/全モーダルの生成・既存機能・個人設定の保存・引用符を含む入力）
- 実ブラウザ（headless Edge・`file://`）シナリオ **33項目すべてOK**
- **自動生成の決定性を維持**: 3回連続生成で全店226件が完全一致／リセット＋生成を3回連続でも一致／実ブラウザでも3回一致
- 印刷: 月間シフト表＝**A4横1枚**、スタッフ個人分＝**A4縦1枚**（PDF出力で確認）。印刷メディアで `.fw` / `.fw-chip` / 個人設定リンクが `display:none` であることを実測

## [1.3.0] - 2026-08-10

### 追加
- **共通設定**（オーナー画面 → 「⚙ 共通設定」）を新設。全店舗に効く設定の置き場
- **雇用形態ごとの既定勤務時間**（実働・休憩）を共通設定で編集可能に。既定値は 店長/社員＝実働8時間＋休憩60分（＝拘束9時間）、パート＝実働5時間、学生＝実働4時間
  - 自動生成の「1コマの長さ」の初期値として使う（例: 社員は 10:00-19:00）。**店長はセルを開いて任意の時刻に変更できる**（既定は初期値であって固定ではない）
- **シフトパターンの登録UI**（共通設定内）。名前（自由入力）・開始・終了・**休憩時間**・色の追加/編集/削除
  - 名前の重複、終了≦開始、休憩≧拘束時間は保存時に止める
  - 削除時は使用中のシフト/希望の件数を出して確認。削除しても時刻は残す（シフトはパターン名が外れる／希望は時間指定に変換される）
- **店舗ごとの使用パターン**を共通設定でチェックボックス選択。**自由時間制の店舗でもパターンを使える**（あてはめてから時刻を微調整できる）
- セル編集モーダルに **休憩・実働の表示**を追加（例: 「休憩 60分（パターンの設定）・実働 8h」／シフト未登録時は「社員の既定: 実働 8h＋休憩 60分 ＝ 拘束 9h」）
- 希望提出モーダルでも、店舗にパターンがあれば自由時間制の店舗でパターン希望を出せるようにした

### 変更（データ構造）
- **パターンを店舗から切り離し、全店共通の1セットに変更**。`patterns[].storeId` を廃止し、`stores[].patternIds`（使うパターンのID一覧）を追加
  - 既存の早番/遅番データは 早番09:00-17:00 / 遅番13:00-21:00 / 中番10:00-18:00 / 夜番14:00-22:00 / 遅め11:00-19:00 の5パターンに整理して移行（**駅前店・モール店の時刻と割合は従来と同一**。モール店の旧「早番10-18」＝中番、旧「遅番14-22」＝夜番）
  - 本店（自由時間制）にも中番・遅めを割り当て、パターンが使えることをデモできるようにした
- パターンに `breakMin`（休憩）を追加。**休憩はパターンの設定が優先、無い場合は雇用形態ごとの既定**を使う
- 条件設定の「パターン割合（%）」のキーを**パターン名からパターンIDに変更**（共通設定で名前を変えても割合が崩れないため）
- 自動生成: 自由時間制の1コマの長さを固定8時間から**割り当てる人の雇用形態の既定（実働＋休憩）**に変更。最短4時間の下限は維持（既定がそれより短い人はその既定まで）
- `state` に **`createdMonths`（自動生成/公開を行った月）** を追加。localStorage キーを `ssd:state:v2` → **`ssd:state:v3`**（構造変更のため再シード）
- 起動時に旧キー `ssd:state:v1` / `ssd:state:v2` を削除する
- 設定モーダルのバージョン表記を v1.2 → v1.3

### 修正（前回レビューの指摘）
- **要修正1**: 「シフトを削除（公休にする）」で、希望のある日が公休にならず `<span class="rq">希望 10:00 15:00</span>` に戻っていた（印刷にも出るため紙面で勤務時間と誤読される恐れ）。セル描画の分岐順を入れ替え、**作成済みの月では希望より公休を優先**するようにした。実測: 削除後のセル `.rq` 1件 → **0件**（グリッド全体でも0件）
- **要修正2**: 未作成の月に手動で1セル入力するとグリッド全体が公休で埋まっていた（公休 5→146/150セル）。判定を「シフト件数 > 0」から **`createdMonths` による明示フラグ**に変更。実測: 1件入力後の公休セル **146/150 → 0/150**（自動生成後は60/150）
- **推奨1**: 有給の日にシフトが入ると、店長にはシフト・スタッフには有給と表示が食い違い、本人が勤務に気づけなかった。スタッフ側も**公開済みシフトを優先表示し、有給を併記**する
- **推奨2**: 公開済みの月で「有給を取り消す」ときだけ本人に通知が飛ばなかった（登録時は通知していた）。取り消しでも差分通知を送る（実測: 通知増分 0 → 1）
- **推奨3**: 未作成の月で有給を取り消したとき、実際は空欄になるのにトーストが「公休になります」と言っていた。月の状態に合わせて「この日はまだ未定に戻ります」に出し分け
- **推奨4**: 未充足の `any`（終日可）希望が作成済みの月に残り印刷されていた。要修正1と同じ分岐順の修正で解消（実測: グリッド内の「終日可」0件・公休表示に統一）
- **推奨5**: 旧 `ssd:state:v1` が端末に残り続けていた。起動時に削除（実測: リセット後に残るキーは `ssd:state:v3` のみ）
- **推奨6**: グリッド下の注記「グレーはスタッフから提出された希望」が、公休チップも同じグレーになったため実態と合わなくなっていた。文言を作成済み/未作成の月の違いに合わせて書き換え

### 検証
- 自動生成の**決定性を維持**（3回連続で全店の生成結果が完全一致・226件）。乱数は不使用
- ヘッドレスブラウザ（Edge）で `file://` から実ファイルを読み込み28項目を実機確認。印刷は **店長=A4横1枚・スタッフ個人分=A4縦1枚**（PDF出力のページ数・用紙サイズで確認）
- 既存機能の退行なし（印刷／CSV列仕様／公休・有給／希望休3日上限／視点切替／公開・通知／人員不足バッジ／リセット）

## [1.2.0] - 2026-08-10

### 追加
- **休みを空欄にせず「公休」と表示**（月間シフト表グリッド／スタッフのシフト確認カレンダー・印刷も同じ）。白黒印刷でも判別できるよう色に頼らず文字で示す
- **有給**を導入。有給の日はグリッド・カレンダーとも「有給」と表示（公休は点線枠、有給は実線枠で印刷時も区別可）
- 有給の入力経路は2つ。スタッフ: 希望提出画面の「🏖 有給申請」／店長: グリッドのセル編集の「🏖 有給にする」。**どちらも `requests` の `type:"paid_leave"` として同一のデータで保持**（店長側は取り消しも可）
- **希望休は月3日まで**（`MAX_DAY_OFF`）。4日目の提出はトーストで理由を出して中止する。**有給はこの上限に含めない**
- 希望提出画面とモーダルに「希望休 N / 3日（有給は上限に含みません）」の残数表示を追加
- デモデータに有給3件（山本 典子・渡辺 健太・井上 拓海）と、希望休を上限いっぱい提出済みのスタッフ（鈴木 恵子＝3日）を追加。**4日目が止まる様子を商談でそのまま見せられる**

### 変更
- 自動生成: **有給を新たに割り当てない・既存の有給を消さない/上書きしない**（希望休と同じ絶対条件）。触れるのは通常シフトと公休のみ。乱数なしの決定的実装は維持（3回連続生成・3回連続リセット＋生成のいずれも結果が完全一致することを実測）
- 自動生成: **有給の日は「月の公休日数」に含めない**。公休を設定どおり確保するため、有給日数のぶん本人の勤務可能日数を減らす（有給を取るほど公休が減る形にしない）
- グリッドで「希望休が通った日」は**公休として表示**し、紙面に「希望休」と「公休」が二重に出ないようにした。由来が分かる補助表示（小さく「希望」）は画面のみで、印刷では出さない
- 割当なし＝公休の表示は**シフト作成済みの月のみ**（未作成の月は従来どおり希望のみ表示。全日「公休」に見えるのを防ぐ）
- スタッフのシフト確認カレンダーの公休表示は**公開済みの月のみ**（未公開の月は空欄のまま）。有給は公開前でも表示する
- セル編集の「シフトを削除」の文言を「シフトを削除（公休にする）」に変更
- 希望の種別に `paid_leave` を追加したため localStorage のキーを `ssd:state:v1` → `ssd:state:v2` に更新（自動で再シード）
- 設定モーダルのバージョン表記を v1.1 → v1.2

### 修正（前回レビューの指摘 R1〜R3）
- R1: 印刷時に `.cal-head` を一律で消していたため、**スタッフの「希望提出」タブをCtrl+Pすると月見出しが紙面から欠落**していた。`.print-head ~ .cal-head` に変更し、印刷見出しを持つ画面でのみ消すようにした
- R2: オーナーダッシュボードの印刷で `.card` のリセットにより3店舗のカードが枠も余白もなく密着していた。印刷時の `.own-card` に枠線と余白を追加
- R3: 下書き（未公開）のCSVがファイル名・中身とも確定版と同一だった。下書きが1件でもある場合は **`シフト_{店舗名}_{YYYY-MM}_下書き.csv`** で保存する（列仕様は変更なし）

## [1.1.0] - 2026-08-10

### 追加
- 店長: 月間シフト表の「印刷」ボタン（グリッド上部のツールバー）。`window.print()` のみで外部ライブラリなし
- 印刷レイアウト（`@media print`）: 画面操作用UI（ヘッダー・視点バッジ・視点切替・下部タブナビ・条件設定・自動生成・公開・印刷/CSVボタン・画面向けの注記）を印刷対象から除外
- 印刷用の見出しブロック（画面には出さない）: 店舗名／対象月／出力日時／状態（下書き・公開済み・未作成）。テナント名も併記
- **下書き（未公開）で印刷した場合は紙面に「下書き（未公開）」を枠囲みで明示**（確定版との取り違え防止）。日付ヘッダー行のスタッフ列にも状態を出し、改ページしても各ページに残るようにした
- 月間シフト表はA4横。`@page { size:A4 landscape; margin:10mm }`、`table-layout:fixed` で31日分を1枚に収める
- スタッフ数が多く1枚に収まらない場合はスタッフ行単位で改ページ（`break-inside:avoid`）し、日付ヘッダー行を各ページで繰り返す（`thead` + `display:table-header-group`）
- 白黒印刷対応: 印刷時のチップは白地＋黒文字にし、店舗色・パターン色は左端の色帯で残す。**パターン名と開始/終了時刻を必ず併記**し、色だけで早番/遅番を区別しない
- スタッフ: シフト確認カレンダーに「このシフトを印刷」ボタン。A4縦1枚で出力（見出しにスタッフ名／店舗名／対象月／出力日時）
- 店長: 「CSVダウンロード」ボタン。1行=1シフト、列は 店舗名／日付／スタッフ名／開始時刻／終了時刻／パターン名（自由時間制の店舗はパターン名が空欄）
- CSVはUTF-8 BOM付き・CRLF改行（Excelでの文字化け防止）。生成は Blob + `URL.createObjectURL()` でクライアント側完結（外部通信なし・`file://` でも動作）
- CSVインジェクション対策（値が `=` `+` `-` `@` で始まる場合はシングルクォートを前置）、カンマ・ダブルクォート・改行を含む値は `"` 囲み＋内部の `"` を `""` にエスケープ
- CSVのファイル名は `シフト_{店舗名}_{YYYY-MM}.csv`（Windowsで使えない文字は `_` に置換）
- ブラウザメニューやCtrl+Pからの印刷でも「出力日時」が最新になるよう `beforeprint` で更新

### 変更
- 設定モーダルのバージョン表記を v1.0 → v1.1

## [1.0.0] - 2026-07-08

### 追加
- 初回リリース（営業デモ用・静的2ファイル構成）
- 視点切替（オーナー／店長×3店舗／スタッフ15名・認証なし）
- スタッフ: 月カレンダーでの公開シフト確認・日タップで店舗全体の体制表示
- スタッフ: 希望休・希望時間（自由時間制）・希望パターン（パターン制）の提出／修正／取り下げ、提出トースト
- 店長: スタッフ×日付の月間シフト表グリッド（今月/来月切替・横スクロール・スタッフ列固定）
- 店長: 提出済み希望のグリッド自動反映表示（希望休=グレー、希望時間=薄字）
- 店長: セルタップでの手動入力・編集・削除（パターン制でも時刻の個別調整可）
- 店長: シフト自動生成（貪欲法・乱数なし。希望休/週上限/連勤上限/公休数/出勤可能曜日を考慮、パターン割合・時間帯別必要人数対応、最短4時間・開店側/閉店側交互の自然な割り当て）
- 店長: 下書き→一括公開（全スタッフへアプリ内通知）、公開後の個別変更で本人のみへ差分通知
- 店長: 条件設定（公休日数・最大連勤・平日/土日必要人数・パターン割合・時間帯別必要人数）
- オーナー: 3店舗横断ダッシュボード（来月シフト状況・希望提出数）、店舗ドリルイン
- 人員不足日の「⚠️不足」バッジ表示
- アプリ内通知（未読バッジ・既読管理）
- デモデータにリセット機能（設定⚙内・確認モーダル付き）
- デモデータの日付を「今日」基準で動的生成（今月=公開済みシフト投入、来月=希望のみ投入）
- GitHub Pages公開・QRコード（qr.png）
